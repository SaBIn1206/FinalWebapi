// Shared currency configuration mirrored from the backend.
// Keeping a single source of truth makes future currency changes trivial.
export const CURRENCY = {
  code: 'NPR',
  symbol: 'Rs',
  locale: 'ne-NP',
} as const;
